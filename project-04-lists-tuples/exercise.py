"""
Project 04: Data Structures I – Lists and Tuples

Complete the functions below according to their docstrings.
Run pytest test.py -v to test your solutions.
"""


def find_minimum(numbers):
    """
    Find the minimum value in a list.
    
    Args:
        numbers (list): List of numbers
    
    Returns:
        int or float: Minimum value, or None if list is empty
    """
    # TODO: Find minimum value
    if not numbers:
        return None
    return None


def find_maximum(numbers):
    """
    Find the maximum value in a list.
    
    Args:
        numbers (list): List of numbers
    
    Returns:
        int or float: Maximum value, or None if list is empty
    """
    # TODO: Find maximum value
    if not numbers:
        return None
    return None


def calculate_average(numbers):
    """
    Calculate the average of numbers in a list.
    
    Args:
        numbers (list): List of numbers
    
    Returns:
        float: Average value, or None if list is empty
    """
    # TODO: Calculate average
    if not numbers:
        return None
    return None


def merge_sorted_lists(list1, list2):
    """
    Merge two sorted lists into one sorted list.
    
    Args:
        list1 (list): First sorted list
        list2 (list): Second sorted list
    
    Returns:
        list: Merged sorted list
    """
    # TODO: Merge two sorted lists
    # Hint: Use two pointers/indexes
    merged = []
    return merged


def reverse_list(items):
    """
    Reverse a list in-place (modify the original list).
    
    Args:
        items (list): List to reverse
    
    Returns:
        None: Function modifies list in-place
    """
    # TODO: Reverse list in-place
    # Hint: Swap elements from ends moving toward center
    pass


def remove_duplicates(items):
    """
    Remove duplicates from a list while preserving order.
    
    Args:
        items (list): List with possible duplicates
    
    Returns:
        list: List with duplicates removed
    """
    # TODO: Remove duplicates
    # Hint: Keep track of seen items
    result = []
    return result


def list_comprehension_squares(n):
    """
    Create a list of squares from 0 to n-1 using list comprehension.
    
    Args:
        n (int): Number of squares to generate
    
    Returns:
        list: List of squares [0, 1, 4, 9, ..., (n-1)^2]
    """
    # TODO: Use list comprehension
    return []


def get_even_numbers(numbers):
    """
    Filter even numbers from a list using list comprehension.
    
    Args:
        numbers (list): List of integers
    
    Returns:
        list: List containing only even numbers
    """
    # TODO: Use list comprehension with condition
    return []


def tuple_operations():
    """
    Demonstrate tuple creation and unpacking.
    
    Returns:
        tuple: A tuple containing (sum, product) of (3, 4)
    """
    # TODO: Create tuple, unpack it, calculate sum and product
    my_tuple = None
    # TODO: Unpack tuple into variables
    a, b = None, None
    # TODO: Calculate sum and product
    return None, None

